import { OddDirective } from './odd.directive';

describe('OddDirective', () => {
  it('should create an instance', () => {
    const directive = new OddDirective();
    expect(directive).toBeTruthy();
  });
});
